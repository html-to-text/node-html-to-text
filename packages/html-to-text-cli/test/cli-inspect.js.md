# Snapshot report for `test/cli-inspect.js`

The actual snapshot is saved in `cli-inspect.js.snap`.

Generated by [AVA](https://avajs.dev).

## Just cli options

> ```
> html-to-text inspect --wordwrap=42 --longWordSplit.forceWrapOnLimit --selectors[] {} :selector=td :format=paragraph
> ```

    `{␊
      wordwrap: 42,␊
      longWordSplit: { forceWrapOnLimit: true },␊
      selectors: [ { selector: 'td', format: 'paragraph' } ]␊
    }␊
    `

## Machine preset followed by cli options

> ```
> html-to-text inspect preset machine --wordwrap=42 --longWordSplit.forceWrapOnLimit --selectors[] {} :selector=td :format=paragraph
> ```

    `{␊
      wordwrap: 42,␊
      longWordSplit: { forceWrapOnLimit: true },␊
      selectors: [␊
        { selector: 'table', format: 'block' },␊
        { selector: 'tr', format: 'block' },␊
        { selector: 'th', format: 'block' },␊
        { selector: 'td', format: 'paragraph' }␊
      ]␊
    }␊
    `

## Json config followed by cli options

> ```
> html-to-text inspect json test/fixtures/options-sample-1.json --wordwrap=42 --longWordSplit.forceWrapOnLimit --selectors[] {} :selector=tr :format=inline
> ```

    `{␊
      selectors: [␊
        { selector: 'p', format: 'paragraph' },␊
        { selector: 'tr', format: 'inline' }␊
      ],␊
      longWordSplit: { wrapCharacters: [ '.', ',' ], forceWrapOnLimit: true },␊
      wordwrap: 42␊
    }␊
    `

## Machine preset followed by json config

> ```
> html-to-text inspect preset machine json test/fixtures/options-sample-1.json
> ```

    `{␊
      wordwrap: 99,␊
      longWordSplit: { forceWrapOnLimit: false, wrapCharacters: [ '.', ',' ] },␊
      selectors: [␊
        { selector: 'table', format: 'block' },␊
        { selector: 'th', format: 'block' },␊
        { selector: 'td', format: 'block' },␊
        { selector: 'tr', format: 'paragraph' },␊
        { selector: 'p', format: 'paragraph' }␊
      ]␊
    }␊
    `

## Json config followed by machine preset

> ```
> html-to-text inspect json test/fixtures/options-sample-1.json preset machine
> ```

    `{␊
      selectors: [␊
        { selector: 'p', format: 'paragraph' },␊
        { selector: 'table', format: 'block' },␊
        { selector: 'tr', format: 'block' },␊
        { selector: 'th', format: 'block' },␊
        { selector: 'td', format: 'block' }␊
      ],␊
      longWordSplit: { wrapCharacters: [ '.', ',' ], forceWrapOnLimit: false },␊
      wordwrap: false␊
    }␊
    `

## Json config 1 followed by json config 2

> ```
> html-to-text inspect json test/fixtures/options-sample-1.json json test/fixtures/options-sample-2.json
> ```

    `{␊
      selectors: [␊
        { selector: 'p', format: 'paragraph' },␊
        { selector: 'tr', format: 'inline' },␊
        { selector: 'td', format: 'inline' }␊
      ],␊
      longWordSplit: { wrapCharacters: [ '/' ] },␊
      wordwrap: 99␊
    }␊
    `

## Json config 2 followed by json config 1

> ```
> html-to-text inspect json test/fixtures/options-sample-2.json json test/fixtures/options-sample-1.json
> ```

    `{␊
      selectors: [␊
        { selector: 'td', format: 'inline' },␊
        { selector: 'tr', format: 'paragraph' },␊
        { selector: 'p', format: 'paragraph' }␊
      ],␊
      longWordSplit: { wrapCharacters: [ '.', ',' ] },␊
      wordwrap: 99␊
    }␊
    `
