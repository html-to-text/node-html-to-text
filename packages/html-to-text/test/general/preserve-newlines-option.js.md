# Snapshot report for `test/general/preserve-newlines-option.js`

The actual snapshot is saved in `preserve-newlines-option.js.snap`.

Generated by [AVA](https://avajs.dev).

## should not preserve newlines by default

> ```html
> <p
> >One
> Two
> Three</p>
> ```

    'One Two Three'

## should preserve newlines when provided with a truthy value

> ```html
> <p
> >One
> Two
> Three</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `One␊
    Two␊
    Three`

## should preserve line feeds in a long wrapping string containing line feeds

> ```html
> <p>If a word with a line feed exists over the line feed boundary then
> you
> must
> respect it.</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `If a word with a line feed exists over the line feed boundary then␊
    you␊
    must␊
    respect it.`

## should preserve line feeds in a long string containing line feeds across the wrap

> ```html
> <p>If a word with a line feed exists over the line feed boundary then
> you must respect it.</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `If a word with a line feed exists over the line feed boundary then␊
    you must respect it.`

## should preserve line feeds in a long string containing line feeds across the wrap with a line feed before 80 chars

> ```html
> <p>This string is meant to test if a string is split properly across a
> newlineandlongword with following text.</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `This string is meant to test if a string is split properly across a␊
    newlineandlongword with following text.`

## should preserve line feeds in a long string containing line feeds across the wrap with a line feed after 80 chars

> ```html
> <p>This string is meant to test if a string is split properly across anewlineandlong
> word with following text.</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `This string is meant to test if a string is split properly across␊
    anewlineandlong␊
    word with following text.`

## should split long lines

> ```html
> <p>If a word with a line feed exists over the line feed boundary then you must respect it.</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `If a word with a line feed exists over the line feed boundary then you must␊
    respect it.`

## should remove spaces if they occur around line feed

> ```html
> <p>A string of text
> with 
> multiple
>  spaces   
>    that 
>  
>  can be safely removed.</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `A string of text␊
    with␊
    multiple␊
    spaces␊
    that␊
    ␊
    can be safely removed.`

## should remove spaces if they occur around line feed 2

> ```html
> multiple
>  spaces
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `multiple␊
    spaces`

## should account for trailing/leading linebreaks of adjacent blocks equally

> ```html
> <p>A</p>
> <div>B</div>
> <div>C</div>
> <p>D</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `A␊
    ␊
    ␊
    B␊
    ␊
    C␊
    ␊
    ␊
    D`

## should work with multiple linebreaks and in presence of whitespaces

> ```html
> <p>A</p> 
>  
>  <p>B</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `A␊
    ␊
    ␊
    ␊
    B`

## should have no special behavior in presence of words among linebreaks

> ```html
> <p>A</p> 
>  B 
>  <p>C</p>
> ```
> 
> ```options
> {
>   "preserveNewlines": true
> }
> ```

    `A␊
    ␊
    ␊
    B␊
    ␊
    ␊
    C`
